import { Config } from 'tailwindcss';

const config = {
  theme: {
    extend: {
      colors: {
        primary: 'black',
        secondary: '#fff',
        primary2: '#d1d1d1',
      },
    },
  },
  plugins: [],
};

export default config;
